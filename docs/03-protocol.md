# Protocol

Purpose: client-broker contract.

Must include:
- transport choice (TCP/HTTP)
- framing format
- request/response schemas
- error codes
- retry semantics
- versioning policy

Must answer:

“If I write a client tomorrow, what can I rely on?”