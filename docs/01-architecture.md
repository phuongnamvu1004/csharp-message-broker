# Architecture

Purpose: system decomposition.

Must include:
- high-level component diagram
- responsibilities of each component
- who owns what state
- concurrency model (single writer, actors, etc.)